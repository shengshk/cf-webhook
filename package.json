{
  "name": "cfwebhook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: forward POST body to Telegram",
  "scripts": {
    "build": "bun ./esbuild.config.js",
    "dev": "wrangler dev",
    "deploy": "bun run build && wrangler deploy --keep-vars"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.107.0"
  }
}
